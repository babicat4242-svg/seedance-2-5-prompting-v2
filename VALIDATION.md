# v2 배포 검증

검증일: 2026-09-12. 실제 영상 생성은 실행하지 않았습니다.

## 패키지 검사

- 기본 2.5 스킬의 문자 수·수정 계약 테스트: **9 passed**.
- EyeCandy 기술·분류 계약 검사: **706/706 passed**.
- 포함한 네 스킬의 `skill-creator` 구조 검사: 모두 통과.
- 스킬의 내부 Markdown 링크 **71개**가 묶음 안에서 해석됨.
- 배포 스킬 본문에 개인 PC 절대 경로가 남아 있지 않음.
- 원본 강의 ZIP/PDF/이미지/영상과 임시 실행 산출물은 배포에 미포함.

## 프롬프트 작성 사례 점검

로컬 v2 작성 규칙에서 확인한 내용:

- 고정 카메라에서 요청된 랙 포커스와 물리적 종소리를 유지.
- 원치 않는 종소리 이름은 음향 부정문에서도 제거하면서 화면의 종탑은 유지.
- 손잡이를 이미 쥔 시작 상태에서 접촉·힘·문 열림을 연결.
- 첫 실패부터 현재 지시만 담은 전체 교체 프롬프트 반환.
- 요청된 돌리 줌, 원본 음원 전용, 원래 길이와 참조 역할 보존.
- 교안의 API 가격·바이트·실패 횟수를 보편 사양으로 가져오지 않음.
- 모든 복사용 카메라 템플릿이 렌즈/참조 원근과 주 이동을 먼저 제시하며 필요한 필드를 유지.

포장 시 EyeCandy 모티프 참조는 묶음 안으로 이동했고, 길이·컷 정책·기본 출력 계약을 따르도록 연결했습니다. 설치된 다른 HERA 스킬을 요구하지 않습니다.

## 재실행

```bash
python -m pytest skills/seedance-2-5-prompting/tests -q
python skills/eyecandy-visual-development/tests/test_technique_contract.py
```

## 2.0.1 길이 통합 점검

20초 원샷과 정확한 참조 태그, 이미지 기반 10초 한국어 고정 숏, 같은 구성의 15초 재작성, 총 25초를 보존하는 명시적 분할 사례를 확인했습니다. 시간 때문에 Fast로 바꾸거나 고정 10초 단위를 강제하지 않습니다.

## 2.0.2 장면별 지시 선택 점검

- 담백한 10초 고정 원샷을 변경 전후 실제 작성해 반복 음향 문구·장식 반응·불필요한 화면 좌우 지정을 제거하고 길이·행동·컷·허용 음향·끝 상태가 유지되는지 비교했습니다.
- 첫 결과의 원치 않는 줌 수리에서는 전체 프롬프트를 다시 작성하고 카메라 이외의 문자열이 동일한지 대조했습니다.
- 좁은 복도 추격의 동선·접촉·통과 여유, 음향만 바꾸는 수정의 태그·시간 보존, 명시한 아크·랙 포커스·두 조명 변화의 보존은 문서 규칙으로 검토했습니다. 이 세 경우의 실제 영상이나 사용자 프롬프트를 대조한 검사는 아닙니다.
- 원본 트랙 전용·무반주 노래·완전 무음의 서로 다른 허용 범위가 간결화 규칙과 충돌하지 않는지 확인했습니다.
- 설치본과 배포본의 변경된 스킬 문서 8개 일치, 세 스킬의 구조, 내부 링크 62개를 확인했습니다. EyeCandy 기법 데이터와 검사 코드는 변경하지 않았습니다.

위 결과는 작성·규칙 검증이며 실제 영상 생성 품질이나 성공률을 측정하지 않았습니다.

## 2.1.0 범용 VFX 배포 점검

- 씨댄스 기본 테스트를 다시 실행해 9 passed, EyeCandy 계약 검사 706/706 passed를 확인했습니다.
- 네 스킬의 구조 검사, 내부 Markdown 링크 71개, 배포 폴더 밖으로 나가는 로컬 문서 참조와 개인 PC 절대 경로 부재를 확인했습니다.
- 범용 VFX 및 씨댄스 연결 파일 9개가 검증한 로컬 설치본과 SHA-256으로 일치합니다.
- 모의 작성에서 계정·플러그인 없는 영화의 레이어 분리 장면과 인물 없는 향수 부유 광고의 완전한 프롬프트를 확인했습니다.
- 씨댄스 2.5의 15초 분신 뮤직비디오에 실제 태그 @Image7·@Audio2, 원본 음원 전용, 무편집 조건을 보존하는 연결 방식을 점검했습니다. 음원을 듣지 않은 상태에서 BPM이나 박자 타임코드를 만들지 않습니다.
- 같은 효과의 영화·광고·뮤직비디오 활용과 효과가 불필요한 사실적 장면의 선택을 문서와 모의 응답에서 확인했습니다.
- 위 항목은 패키지 및 프롬프트 작성 점검입니다. 실제 영상 생성·모델별 재현 성능 테스트는 실행하지 않았습니다.
